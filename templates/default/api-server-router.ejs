'use strict';
// @CodeGeneratorOverwrite: enabled
const express = require('express');
const authController = require("./controllers/auth-controller.js");<% 
swUtil.controllers.forEach((p)=>{%>
const <%-p.controlCode%> = require("./<%- path.relative(path.join(scope["api-server-router"], ".."), p["api-server-controller"]).split("\\").join("/") %>");<%}) %>
let router = express.Router();

<%swUtil.operations.forEach((p)=>{%> 
router.<%- p.method.toLowerCase() %>(<%-JSON.stringify(p.path.split("{").join(":").split("}").join(""))%>, <%- p.controlCode %>.<%- p.operationCode %> );<%
})%>

module.exports = router;