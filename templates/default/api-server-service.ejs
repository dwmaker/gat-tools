'use strict';
// @CodeGeneratorOverwrite: enabled

let service = {};
<% swUtil.operations.filter(o=>o.controlCode == scope.controlCode).forEach((operation)=>{%>
service.<%= operation.operationCode %> = 
function(<%= swUtil.parameters.filter((p)=> p.operationCode == operation.operationCode && p.controlCode == scope.controlCode).map(p=> p.name).join(", ") %>)
{
	return new Promise((resolve, reject) =>
	{
		reject("funcionalidade não implementada");
	});
};
<%})%>
module.exports = service;